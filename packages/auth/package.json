{
  "name": "@dowze/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Client Supabase partagé de Dowze — session cookie .dowze.ch (SSO entre academie et plugins), hooks de session & logout global.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
